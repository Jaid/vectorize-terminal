import type {Options} from '../../../src/main.ts'
import tabIcon from '../tabIcon.ts'

export default {
  decoration: {
    type: 'windowsTerminal',
    tabTitle: "PowerShell",
    tabIcon
  }
} satisfies Options
